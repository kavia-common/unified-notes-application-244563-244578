# Environment variables required for FastAPI backend

# CRITICAL: Do NOT share secrets in production or version control!
POSTGRES_URL=localhost
POSTGRES_USER=postgres
POSTGRES_PASSWORD=yourpassword
POSTGRES_DB=notesdb
POSTGRES_PORT=5432

SECRET_KEY=change_this_secret_to_a_random_long_value_for_jwt_token
